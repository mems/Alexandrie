#!/usr/bin/env sh

# Only staged files
files=$(git diff --cached --name-only --diff-filter=ACM | grep "^data/AX[0-9A-F]\+\.md$")

if [ "$files" = "" ]; then
	exit 0
fi

for file in ${files}; do
	./axlint < ./$file > ./$file
done